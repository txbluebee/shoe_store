<h1> All Brands</h1>
<% if @brands.any?() %>
  <ul>
    <% @brands.each() do |brand| %>
      <li><%= brand.name() + ": " + "$" + brand.price().round(2).to_s()%> </a></li>
    <% end %>
  </ul>
<% else %>
  <h4>No stores added yet.</h4>
<% end %>
