<h1> All Stores</h1>
<% if @stores.any?() %>
  <ul>
    <% @stores.each() do |store| %>
      <li><a href="/stores/view/<%= store.id() %>"><%= store.name() %></a></li>
    <% end %>
  </ul>
<% else %>
  <h4>No stores added yet.</h4>
<% end %>
